"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import { BudgetNav } from "@/components/budget-nav"; import { useEffect,useState } from "react";
export default function Clients(){const[list,setList]=useState<{id:string;name:string;document:string|null;email:string|null}[]>([]);const[name,setName]=useState('');const[email,setEmail]=useState('');async function load(){const r=await fetch('/api/clients');if(r.ok)setList(await r.json())}useEffect(()=>{void load()},[]);async function save(){const r=await fetch('/api/clients',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email:email||null})});if(r.ok){setName('');setEmail('');load()}}return <main className="min-h-screen bg-stone-50"><BudgetNav/><div className="mx-auto max-w-5xl p-4"><h1 className="text-2xl font-bold">Clientes</h1><div className="mt-4 flex gap-2 rounded-2xl bg-white p-4"><input className="input" placeholder="Nome do cliente" value={name} onChange={e=>setName(e.target.value)}/><input className="input" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><button className="button-primary" disabled={!name} onClick={save}>Cadastrar</button></div><div className="mt-4 space-y-2">{list.map(x=><div className="rounded-xl bg-white p-4" key={x.id}><strong>{x.name}</strong><span className="ml-2 text-sm text-stone-500">{x.email}</span></div>)}</div></div></main>}
